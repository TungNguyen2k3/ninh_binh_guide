[build]
builder = "NIXPACKS"
buildCommand = "npm install && npm run build"

[deploy]
startCommand = "npx prisma db push && npx tsx prisma/seed.ts && node dist/app.js"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
